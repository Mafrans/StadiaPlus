import React from 'react';
import styled from 'styled-components';
import tw from 'twin.macro';

export default function OnboardPage() {
    return <Main>

    </Main>;
}

const Main = styled.main(tw``)